{
  "name": "predictai-agent",
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0",
    "@mysten/sui": "latest",
    "@mysten-incubation/memwal": "latest",
    "node-fetch": "^3.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "dev": "node --watch dist/index.js"
  }
}
